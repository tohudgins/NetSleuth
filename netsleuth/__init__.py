"""NetSleuth — a defensive, educational network security toolkit.

Two integrated modules:
  * scanner  — from-scratch TCP/UDP port scanner (socket connect + scapy SYN),
               banner grabbing, and an OS *family heuristic*.
  * sniffer  — scapy-based live packet capture and protocol analyzer.

Authorized use only: run only against systems you own or have explicit
permission to test. See the README.
"""

__version__ = "0.1.0"
